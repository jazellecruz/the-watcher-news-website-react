import "../styles/customStyles.css";

export default function Hero() {
    return (
        <div className="hero-content">
        <h1 className="hero-title">the <span className="hero-title-highlight">Watcher</span></h1>
        <p className="hero-sub-title">the <span className="hero-sub-title-highlight">Watchman</span> for the latest global headlines</p>
        </div>
    );
}